import { IBM_Plex_Sans, Oswald } from 'next/font/google';
export const oswald = Oswald({ subsets: ['latin'], weight: ['600', '300'], });
export const plex = IBM_Plex_Sans({ subsets: ['latin'], weight: ['600', '300'], })
